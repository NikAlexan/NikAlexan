# План действий по updated-project-review

Дата: 2026-01-30

## Цели
- Закрыть критичные замечания (темы и шрифты).
- Зафиксировать единый подход к текстовой прозрачности.
- Сформировать базу для дальнейших улучшений контента и SEO.

## Чеклист действий

### 1) Критично (в первую очередь)
- [x] Синхронизировать механизм тем: выбрать **один** источник истины (класс `dark` или `data-theme`) и привести **CSS + JS** к единому формату.
- [x] Добавить `preconnect` для Google Fonts в `BaseLayout.astro` и проверить отсутствие дублей подключений.

### 2) Важно (на этой неделе)
- [x] Вынести повторяющиеся `color-mix` значения в утилиты (`.text-muted`, `.text-disabled`, `.text-strong`) или в Tailwind-слои и заменить дубли в шаблонах.
- [x] Обновить/дополнить `Content Collections` схемы проектов (company, role, period, tags, category) и согласовать обязательность полей.
- [x] Добавить/проверить 404-страницу и актуальность sitemap (если еще нет).

### 3) Улучшения (следующий спринт)
- [x] Рассмотреть переход `case-studies` на `.mdx` и подготовить 1-2 компонента для вставки в контент.
- [x] Оптимизировать изображения (Astro Image, WebP/AVIF) и проверить качество на реальных кейсах. (N/A: в контенте нет растровых изображений, только иконки)
- [x] Провести mini-a11y аудит (DevTools). Закрыто: invalid href, aria-label для мобильного theme toggle.

### 4) Документация
- [x] Обновить README: процесс добавления проекта и кейса.
- [x] Краткая документация цветовой системы и токенов (`docs/color-system.md`).
- [x] Style guide (минимальный список правил по типографике/цветам/отступам) (`docs/style-guide.md`).

### 5) Контактные ссылки (MDX)
- [x] Вынести ссылки в единый MDX: `src/content/contact/links.mdx`.
- [x] Добавить коллекцию `contact` в `src/content/config.ts`.
- [x] Создать переиспользуемый блок `ContactLinks.astro`.
- [x] Подключить `ContactLinks.astro` в `Footer.astro` и убрать дублирование в `CaseStudyPage.astro`.
- [x] Проверить визуально (главная и кейс).
- [x] Прогнать Lighthouse и убедиться, что нет invalid href.

## Порядок выполнения
Все пункты выполнены и зафиксированы (см. чеклист).

## Критерии готовности
Все критерии выполнены:
- Темы переключаются корректно и единообразно (CSS и JS совпадают).
- Шрифты грузятся через `preconnect` и не дублируются.
- Текстовые прозрачности стандартизированы.
- Схемы и контент коллекций расширены без ошибок типизации.
- Документация отражает актуальные процессы.
