# План действий по updated-project-review

Дата: 2026-01-30

## Цели
- Закрыть критичные замечания (темы и шрифты).
- Зафиксировать единый подход к текстовой прозрачности.
- Сформировать базу для дальнейших улучшений контента и SEO.

## Чеклист действий

### 1) Критично (в первую очередь)
- [ ] Синхронизировать механизм тем: выбрать **один** источник истины (класс `dark` или `data-theme`) и привести **CSS + JS** к единому формату.
- [ ] Добавить `preconnect` для Google Fonts в `BaseLayout.astro` и проверить отсутствие дублей подключений.

### 2) Важно (на этой неделе)
- [ ] Вынести повторяющиеся `color-mix` значения в утилиты (`.text-muted`, `.text-disabled`, `.text-strong`) или в Tailwind-слои и заменить дубли в шаблонах.
- [ ] Обновить/дополнить `Content Collections` схемы проектов (company, role, period, tags, category) и согласовать обязательность полей.
- [ ] Добавить/проверить 404-страницу и актуальность sitemap (если еще нет).

### 3) Улучшения (следующий спринт)
- [ ] Рассмотреть переход `case-studies` на `.mdx` и подготовить 1-2 компонента для вставки в контент.
- [ ] Оптимизировать изображения (Astro Image, WebP/AVIF) и проверить качество на реальных кейсах.
- [ ] Провести mini-a11y аудит (Lighthouse), зафиксировать и закрыть найденные замечания.

### 4) Документация
- [ ] Обновить README: процесс добавления проекта и кейса.
- [ ] Краткая документация цветовой системы и токенов.
- [ ] Style guide (минимальный список правил по типографике/цветам/отступам).

## Порядок выполнения
1. Закрыть пункт темы (CSS/JS) и проверить переключение светлой/темной темы.
2. Добавить `preconnect` для шрифтов и проверить сетевые запросы.
3. Ввести утилиты прозрачности и заменить повторяющиеся `color-mix`.
4. Расширить схему проектов и обновить 1-2 файла для примера.
5. Документация и улучшения по мере готовности.

## Критерии готовности
- Темы переключаются корректно и единообразно (CSS и JS совпадают).
- Шрифты грузятся через `preconnect` и не дублируются.
- Текстовые прозрачности стандартизированы.
- Схемы и контент коллекций расширены без ошибок типизации.
- Документация отражает актуальные процессы.
